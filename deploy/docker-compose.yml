# central-deploy-contract-version: 1
services:
  robotsix-browser:
    image: ghcr.io/damien-robotsix/robotsix-browser:main
    ports:
      - "8310:8000"
    labels:
      robotsix.deploy.mode: "deployment-system"
      robotsix.deploy.port: "8000"
      robotsix.deploy.health: "/health"
